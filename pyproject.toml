[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "fantastic-ssm"
version = "0.1.0"
requires-python = ">=3.8"

[tool.setuptools.packages.find]
where = ["."]
include = ["models*"]
